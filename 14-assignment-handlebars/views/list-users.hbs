<main>
    <h1>List of users</h1>
    {{#if hasUsers}}
        <ul>
            {{#each users}}
                <li>{{ this.name }}</li>
            {{/each}}
        </ul>
    {{else }}
        <h1>No users!</h1>
    {{/if}}
</main>>